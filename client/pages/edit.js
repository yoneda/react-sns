import React from 'react';
import Header from "../components/Header";

const Edit = props => {
    const { num, name } = props;
    return (
        <div>
          <Header />
          edit
        </div>
    );
};

export default Edit;
