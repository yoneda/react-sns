import React from 'react';
import Header from "../components/Header";

const Login = props => {
    const { num, name } = props;
    return (
        <div>
          <Header />
          login
        </div>
    );
};

export default Login;
