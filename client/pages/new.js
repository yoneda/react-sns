import React from 'react';
import Header from "../components/Header";

const New = props => {
    const { num, name } = props;
    return (
        <div>
          <Header />
          new
        </div>
    );
};

export default New;
