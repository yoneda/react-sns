import * as types from "./types";

export const getAddCount = () => ({ type: types.ADD_COUNT })

export const getSubCount = () => ({ type: types.SUB_COUNT })

export const getResetCount = () => ({ type: types.RESET_COUNT })